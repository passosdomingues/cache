/* EXERCÍCIO - DATA DE ENTREGA: 24/03/2026
   OBJETIVO: Reproduzir a busca: the treap and the "randomized binary" search tree
   
   ANÁLISE DA QUERY:
   - "the treap", "and", "the", "search", "tree": Termos individuais (Match).
   - "randomized binary": Frase exata devido às aspas (Match Phrase).
   - A conjunção exige que todos os elementos principais estejam presentes (Must).
*/

GET /wikipedia/_search
{
  "query": {
    "bool": {
      /* 'must' garante que todos os critérios abaixo sejam atendidos (Lógica AND) */
      "must": [
        {
          /* Busca os termos soltos no conteúdo. 
             O analyzer 'analyzer_for_content' cuidará de remover 'the' e 'and' 
             se forem stopwords, ou tratará como termos comuns. */
          "match": {
            "content": "the treap and the search tree"
          }
        },
        {
          /* O equivalente às aspas do Google: busca a frase exata e adjacente. */
          "match_phrase": {
            "content": "randomized binary"
          }
        }
      ],
      /* 'should' opcional: se os termos aparecerem no TÍTULO, o documento 
         sobe no ranking (melhora o score), simulando a relevância do Google. */
      "should": [
        {
          "match": {
            "title": "the treap randomized binary search tree"
          }
        }
      ]
    }
  }
}
