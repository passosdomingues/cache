import { Autocomplete } from './components/autocomplete.js';

// Inicializa os módulos quando o DOM estiver pronto
document.addEventListener('DOMContentLoaded', () => {
    // Instancia o componente de Autocomplete
    new Autocomplete();
});
