GET /wikipedia/_search
{
  "query": {
    "bool": {
      /* O 'must' atua como o filtro rigoroso. 
         Se o documento não passar por aqui, ele nem aparece no resultado.
      */
      "must": [
        {
          "match": {
            "content": {
              "query": "treap", 
              "operator": "and" 
              /* Removendo stop words: focamos apenas em 'treap' */
            }
          }
        },
        {
          /* Simulando as aspas do Google ("randomized binary").
             O match_phrase garante que as palavras estejam juntas e na ordem.
          */
          "match_phrase": {
            "content": "randomized binary"
          }
        },
        {
          /* Incluímos 'search tree' como obrigatório para contextualizar,
             mas sem a rigidez de frase exata no conteúdo todo.
          */
          "match": {
            "content": "search tree"
          }
        }
      ],
      /* O 'should' funciona como o ranking do Google.
         Documentos com esses termos no título aparecerão primeiro.
      */
      "should": [
        {
          "match": {
            "title": "treap"
          }
        },
        {
          "match_phrase": {
            "title": "randomized binary search tree"
          }
        }
      ]
    }
  }
}
